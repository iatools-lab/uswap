VITE_API_URL=http://127.0.0.1:3000

# Optional. Restrict this public browser key to the deployed domain in MapTiler.
# Without it, the station map uses OpenStreetMap tiles and Nominatim geocoding.
VITE_MAPTILER_KEY=
